OPENAI_API_KEY=
ANTHROPIC_API_KEY=
CURSOR_API_KEY=
PORT=8787
HOST=127.0.0.1

# Optional. If set, Settings locks that field (default + cannot change).
# On ECS these come from terraform.tfvars office_provider / office_model / office_floor
# (a local .env is not copied into the image).
# PROVIDER=openai
# MODEL=gpt-4o-mini
# FLOOR=live
